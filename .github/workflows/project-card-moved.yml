on:
  project_card:
    types: [moved]
name: Project Card Event
jobs:
  triage:
    name: Auto card labeler
    runs-on: ubuntu-latest
    steps:
      - uses: takaki/auto-card-labeler@master
#      - uses: technote-space/auto-card-labeler@v1
